spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mysqltut
    username: root
    password: 12345678
#    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: update
  sql:
    init:
      mode: always

